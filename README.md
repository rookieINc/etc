```mermaid
graph TD
    A[原始会议录音文件] --> B[加载音频文件]
    B --> C{音频文件是否有效?}
    C -->|是| D[预处理音频数据]
    C -->|否| E[返回错误: 无效音频文件]
    D --> F[Silero VAD 语音活动检测]
    F --> G{检测到语音段落?}
    G -->|是| H[提取语音段落]
    G -->|否| I[返回错误: 无语音段落]
    H --> J[分段存储语音段落]
    J --> K{是否存在背景噪音?}
    K -->|是| L[加载 RNNoise]
    K -->|否| M[跳过噪声抑制]
    L --> N[噪声抑制处理]
    N --> O[保存去噪音频]
    M --> O
    O --> P[加载 Whisper 模型]
    P --> Q{模型加载成功?}
    Q -->|是| R[Whisper 语音识别]
    Q -->|否| S[返回错误: 模型加载失败]
    R --> T{识别结果是否准确?}
    T -->|是| U[提取识别结果]
    T -->|否| V[人工校验和修改]
    U --> W[分段存储识别结果]
    V --> W
    W --> X[文本后处理]
    X --> Y{是否需要进一步校验?}
    Y -->|是| Z[人工校验和修改]
    Y -->|否| AA[生成最终纪要]
    Z --> AA
    AA --> AB[输出纪要文件]
    AB --> AC[归档和存储]
    AC --> AD[通知相关人员]
